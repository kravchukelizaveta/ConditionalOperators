#найти минимальное расстояние, которое должен проплыть Яша, чтобы выбраться из бассейна на бортик
if __name__ == '__main__':
    n = int(input('Введите длину бассейна'))
    m = int(input('Введите ширину бассейна'))
    x = int(input('Расстояние от одного из длинных бортиков'))
    y = int(input('Расстояние от одного из коротких бортиков'))
    if n > m:
        term = n
        n = m
        m = term
    if x > n / 2:
        x = n - x
    if y > m / 2:
        y = m - y
    if x > y:
        print('y')
    else:
        print('x')
